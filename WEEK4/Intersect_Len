def intersect(self, other):
    result = ''
    self.vals.sort()
    other.vals.sort()
    for e in self.vals:
        if e in other.vals:
            result = result +str(e) + ','
    return '{' + result[:-1] + '}'
        
def __len__(self):
    count = 0
    for e in self.vals:
        count+=1
    return count
